// Gradle is used for managing dependencies and makes the process of collaborating easier
// Gradle can be though of as a "build tool"

plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
